// import Immutable from 'immutable'
import { clearJSON, stubJSONStore } from '../../support/stubs'

describe('comments experience update', () => {
  beforeEach(() => {
    stubJSONStore()
  })

  afterEach(() => {
    clearJSON()
  })

  describe('#updateCommentsCount', () => {
    it('should be tested')
  })

  describe('#addOrUpdateComment', () => {
    it('should be tested')
  })

  describe('#toggleEditing', () => {
    it('should be tested')
  })
})


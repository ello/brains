{
  "name": "ello-brains",
  "version": "1.0.0",
  "description": "The brains of ello's react applications.",
  "main": "index.js",
  "repository": "https://github.com/ello/brains",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "babel src --presets babel-preset-env --out-dir dist",
    "build:watch": "babel src --presets babel-preset-env --out-dir dist --watch",
    "copypackage": "cp -rf package.json dist"
  },
  "devDependencies": {
    "babel-cli": "6.24.1",
    "babel-preset-env": "1.4.0"
  }
}
